{
  "name": "@keel-code/loop",
  "version": "0.0.0",
  "private": true,
  "description": "闭环编排器：批次上报 → 独立 reviewer → 分类路由 → 修复循环 → review-pass → 待决策 → 验收",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@keel-code/engine": "workspace:*",
    "@keel-code/methodology": "workspace:*",
    "@keel-code/roster": "workspace:*"
  },
  "devDependencies": {
    "@keel-code/guards": "workspace:*",
    "@keel-code/testkit": "workspace:*"
  }
}
